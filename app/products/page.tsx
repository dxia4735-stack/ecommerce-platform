'use client';import {useMemo,useState} from 'react';import {products} from '../data';import {ProductCard} from '../ui';
export default function Products(){const[search,setSearch]=useState('');const[cat,setCat]=useState('全部');const[sort,setSort]=useState('推荐');const list=useMemo(()=>{let a=products.filter(p=>(cat==='全部'||p.category===cat)&&(`${p.name}${p.description}`.includes(search)||!search));return sort==='价格从低到高'?[...a].sort((x,y)=>x.price-y.price):sort==='价格从高到低'?[...a].sort((x,y)=>y.price-x.price):a},[search,cat,sort]);return <main className="wrap section"><div className="eyebrow">COLLECTION</div><h1 style={{font:'52px Georgia',margin:'12px 0'}}>全部商品</h1><div className="toolbar"><input className="input" placeholder="搜索商品" value={search} onChange={e=>setSearch(e.target.value)}/><select className="select" value={cat} onChange={e=>setCat(e.target.value)}>{['全部','家居','配饰','生活','香氛','服饰'].map(x=><option key={x}>{x}</option>)}</select><select className="select" value={sort} onChange={e=>setSort(e.target.value)}><option>推荐</option><option>价格从低到高</option><option>价格从高到低</option></select></div><div className="grid">{list.map(p=><ProductCard key={p.id} p={p}/>)}</div>{!list.length&&<p className="muted">没有找到匹配的商品。</p>}</main>}
