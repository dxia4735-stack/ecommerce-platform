'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {useShop,Role} from '../shop-context';
const go=(role:Role)=>role==='seller'?'/seller/dashboard':role==='admin'?'/admin/dashboard':'/index';
export default function Login(){const[account,setAccount]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const{setUser}=useShop();const router=useRouter();const submit=()=>{const saved=JSON.parse(localStorage.getItem(`noma-account-${account}`)||'null');if(!account||password.length<4){setError('请输入账号和至少 4 位密码');return}if(!saved){setError('账号不存在，请先注册');return}if(saved.password!==password){setError('密码不正确');return}setUser(account,saved.role as Role);const next=localStorage.getItem('noma-next');localStorage.removeItem('noma-next');router.push(next||go(saved.role as Role))};return <main className="authPage"><div className="authCard"><div className="authLogo">NOMA</div><p className="authSlogan">让好物回到生活</p><h1>欢迎回来</h1><label>手机号 / 账号</label><input value={account} onChange={e=>setAccount(e.target.value)} placeholder="请输入手机号或账号"/><label>密码 / 验证码</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="请输入密码"/>{error&&<p className="error">{error}</p>}<button className="btn authBtn" onClick={submit}>登录</button><div className="authLinks"><Link href="/register">注册新账号</Link><button onClick={()=>setError('演示模式请联系平台管理员重置密码')}>忘记密码</button></div></div></main>}
