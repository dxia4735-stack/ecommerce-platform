'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {useShop} from '../shop-context';
export default function Checkout(){const{items,user,createOrder}=useShop();const r=useRouter();const[name,setName]=useState('');const[phone,setPhone]=useState('');const[address,setAddress]=useState('');const[coupon,setCoupon]=useState('0');if(!user)return <main className="wrap section"><h1 style={{font:'48px Georgia'}}>请先登录</h1><button className="btn" onClick={()=>r.push('/login')}>前往登录</button></main>;const subtotal=items.reduce((s,x)=>s+x.product.price*x.qty,0);const discount=coupon==='50'?50:0;const total=Math.max(0,subtotal-discount);return <main className="wrap section"><div className="form"><div className="eyebrow">CHECKOUT</div><h1 style={{font:'48px Georgia'}}>确认订单</h1><label>收货人</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="姓名"/><label>手机号码</label><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="11 位手机号"/><label>收货地址</label><input value={address} onChange={e=>setAddress(e.target.value)} placeholder="省市区、街道及门牌号"/><label>优惠券</label><select className="select" value={coupon} onChange={e=>setCoupon(e.target.value)} style={{width:'100%'}}><option value="0">不使用优惠券</option><option value="50">满 500 减 50</option></select><label>支付方式</label><select className="select" style={{width:'100%'}}><option>支付宝（演示）</option><option>微信支付（演示）</option></select><div className="summary" style={{marginLeft:0}}><div style={{display:'flex',justifyContent:'space-between'}}><span>商品小计</span><b>¥{subtotal}</b></div><div style={{display:'flex',justifyContent:'space-between',marginTop:8}}><span>优惠</span><b>-¥{discount}</b></div><div style={{display:'flex',justifyContent:'space-between',marginTop:12}}><span>应付金额</span><b>¥{total}</b></div><button className="btn" style={{marginTop:18}} onClick={()=>{if(name&&phone.length>=11&&address&&items.length){createOrder();r.push('/success')}}}>确认支付（演示）</button></div></div></main>}
